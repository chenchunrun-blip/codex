apiVersion: v1
kind: Service
metadata:
  name: alert-normalizer
  namespace: security-triage
  labels:
    app: security-triage
    component: alert-normalizer
spec:
  type: ClusterIP
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
      name: http
  selector:
    app: security-triage
    component: alert-normalizer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-normalizer
  namespace: security-triage
  labels:
    app: security-triage
    component: alert-normalizer
    version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: security-triage
      component: alert-normalizer
  template:
    metadata:
      labels:
        app: security-triage
        component: alert-normalizer
        version: latest
    spec:
      containers:
        - name: alert-normalizer
          image: security-triage/alert-normalizer:latest
          ports:
            - containerPort: 8002
              name: http
          envFrom:
            - configMapRef:
                name: security-triage-config
            - secretRef:
                name: security-triage-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
