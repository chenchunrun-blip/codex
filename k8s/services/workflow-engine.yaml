apiVersion: v1
kind: Service
metadata:
  name: workflow-engine
  namespace: security-triage
  labels:
    app: security-triage
    component: workflow-engine
spec:
  type: ClusterIP
  ports:
    - port: 8008
      targetPort: 8008
      protocol: TCP
      name: http
  selector:
    app: security-triage
    component: workflow-engine
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-engine
  namespace: security-triage
  labels:
    app: security-triage
    component: workflow-engine
    version: latest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: security-triage
      component: workflow-engine
  template:
    metadata:
      labels:
        app: security-triage
        component: workflow-engine
        version: latest
    spec:
      containers:
        - name: workflow-engine
          image: security-triage/workflow-engine:latest
          ports:
            - containerPort: 8008
              name: http
          envFrom:
            - configMapRef:
                name: security-triage-config
            - secretRef:
                name: security-triage-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
