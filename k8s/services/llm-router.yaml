apiVersion: v1
kind: Service
metadata:
  name: llm-router
  namespace: security-triage
  labels:
    app: security-triage
    component: llm-router
spec:
  type: ClusterIP
  ports:
    - port: 8007
      targetPort: 8007
      protocol: TCP
      name: http
  selector:
    app: security-triage
    component: llm-router
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-router
  namespace: security-triage
  labels:
    app: security-triage
    component: llm-router
    version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: security-triage
      component: llm-router
  template:
    metadata:
      labels:
        app: security-triage
        component: llm-router
        version: latest
    spec:
      containers:
        - name: llm-router
          image: security-triage/llm-router:latest
          ports:
            - containerPort: 8007
              name: http
          envFrom:
            - configMapRef:
                name: security-triage-config
            - secretRef:
                name: security-triage-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8007
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8007
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
